"# Chat-Bot-with-Watson-Assistant" 
# To run frontend:
1. Change directory to "frontend"
2. npm install (one time only)
3. npm start

# To run backend:
1. Run the main class Application.java

# Endpoints:
frontend: http://localhost:4200

backend: http://localhost:8080